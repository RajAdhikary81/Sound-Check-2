# Railway / Render / Koyeb deployment config
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python3 -m MusicBangla"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
